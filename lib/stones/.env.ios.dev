# Needed for ios related stuff
key_id=
issuer_id=
key_content=

# Needed for firebase deploys
ipa_path=
FIREBASE_CLI_TOKEN=
FIREBASE_IOS_APP_ID=
groups=

# Needed for building the app
export_options=

# Flavor config for flutter builds is needed
# For example:
#FLAVOR_CONFIG="--flavor dev -t lib/main_dev.dart"
FLAVOR_CONFIG=